---
  - hosts: all

    tasks:
      - name: Actualizar lista de repositorios e instalar Docker
        become: true
        apt:
          name: docker
          update_cache: yes

      - name: Clonado de imagen de DockerHub (adrianmorente/practicasiv)
        become: true
        command: docker pull adrianmorente/practicasiv
